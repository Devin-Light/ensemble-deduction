---
name: ensemble-deduction
description: 多 agent 协同剧情推演——为每个角色分配独立 agent，注入人设，在导演 agent 的感知路由下完成场景互动推演，编剧（用户）最终裁决。推演者原则从规则约束升级为架构隔离。
triggers:
  - 角色推演
  - 多agent推演
  - 场景推演
  - ensemble
  - 角色模拟
---

# 多 Agent 协同剧情推演

将推演者原则从"规则约束"升级为"架构隔离"——每个角色由独立 agent 饰演，信息边界是物理的而非提示词层面的，视角污染从根源上不发生。

## 架构

```
编剧（用户）→ 设定弧光方向、人设精度、最终裁决
     ↓
导演 Agent → 设置场景、路由感知、记录互动、运用 skill 原则组织成文
     ↓
角色 Agent 群 → Agent-A / Agent-B / Agent-C（只知道自己该知道的事）
     ↓ （互动数据返回）
导演 Agent → 覆盖错误方向、编织行文
     ↓
编剧（用户）→ 审核、精修、确认弧光变更
```

---

## 角色人设注入标准

每个角色的人设按以下分层格式注入 Agent。导演在每章推演前与编剧确认"当前弧光"区块，其余层继承上次。

```
角色名
├── 核心锚点（不变）
│   一句话。贯穿全文。所有行为从这句话推导。
│
├── 当前弧光（每章更新）
│   ├── 本章起点：上一章结束后她的状态
│   ├── 本章关键情绪：此刻带着什么进来
│   └── 本章可能的变化方向：经过本章事件后可能朝哪个方向偏移
│
├── 行为映射表（继承上一章，本章可微调）
│   格式：「在 X 情境下 → Y 反应」
│   不是"性格描述"，是"输入→输出"规则。
│   优先对接元 skill 约束 #14 的叙事呼吸感映射表。
│
├── 语言特征（基本稳定，情绪极度变化时可偏移）
│   ├── 句长范围
│   ├── 语气特征
│   ├── 口头禅/惯用句式
│   └── 从不说：哪些词/句类永远不会出现在她嘴里
│
└── 信息边界（每章更新白名单）
    ├── 已知：她此刻知道的所有信息
    └── 不知：她不应该知道的信息（即使读者/作者已知）
```

### 人设注入示例

```markdown
## Agent-符华 · 第四章起点

### 核心锚点
"赤鸢仙人"是我维持了数千年的秩序框架。
任何扰乱这个框架的事物，都是需要被纠正的。

### 当前弧光（第四章）
- 起点：带首徒下山清理入魔家族，一切如常
- 关键情绪：专业冷静 → 被"华"扰动 → 纠正后恢复秩序
- 可能变化：发现圣痕，决定收为第八徒，将不稳定因素纳入太虚山管控

### 行为映射
- 发现入魔者 → 消灭，不犹豫
- 弟子自行判断 → 默许，不干预（但审视）
- 听到不该被知道的"华" → 纠正，封闭。不追问，不展开
- 看到圣痕 → 视为不稳定因素，纳入管控。"带回太虚山"
- 日常交流 → 指令式，单句，不回看，不解释

### 语言特征
- 句长：极少超过 15 字
- 语气：平，不升不降，不拖尾音
- 从不说：疑问句（审问除外）、感叹句、软化的语气词（"吧""呢""啊"）

### 信息边界
- 已知：太虚剑气全体系、门下七徒、崩坏能感知
- 不知：前文明记忆（已分割）、瑶笙的穿越来历
```

---

## 推演工作流

### 第 1 步：场景初始化（导演）
导演设置场景参数并显式输出：
- 时间点、地点、在场角色
- 每个角色的初始物理位置
- 各角色的视觉/听觉/已知信息边界（B-3 快照）
- 本场景的弧光目标（哪个角色的信息状态会发生变化）

### 第 2 步：首轮推演（导演 → 角色 Agent）
导演选定本轮的行动角色，向其 Agent 发送：
- 场景描述（仅含其可感知的信息）
- 当前上下文（此前的互动结果，仅含其应知晓的部分）
- 动作请求：简短，聚焦本轮需要其反应的刺激

### 第 3 步：角色响应（角色 Agent → 导演）
角色 Agent 返回该角色的反应：动作、对话、或沉默。如 Agent 输出偏离人设或跑偏，导演拒绝并附纠正提示重来。

### 第 4 步：感知广播（导演 → 所有相关角色 Agent）
导演将角色 A 的行动结果广播给所有能感知到该行动的角色：
- 角色 B 能完整看到 A 的动作 → 收到完整描述
- 角色 C 只能模糊感知到 → 收到模糊描述
- 角色 D 完全无法感知 → 不收到任何信息

### 第 5 步：重复 2-4
一轮内可能有多个角色同时收到信息——收到信息的角色各自决定是否反应。有人反应、有人沉默。导演在每轮结束后判断是否继续。

### 第 6 步：记录与编织（导演）
导演将全部互动记录整理成文。运用元 skill 的约束规则：
- 第 8 条推演者原则（感知边界）
- 第 12 条镜头流动感（默认基准）
- 第 14 条叙事呼吸感（关键情感节点打破匀速）
- 第 11 条人称亦是叙事
- 第 10 条少即是多

导演有权覆盖任何角色的输出——当 agent 的方向与编剧设定的大纲冲突时，导演纠正并标注。

### 第 7 步：编剧审核
用户审核成文，调整人设精度、修正弧光方向、注入不可量化的文学判断。确认后，导演记录弧光变更，作为下一章的起点。

---

## 导演职责清单

| 职责 | 说明 |
|---|---|
| 场景控制 | 告知 agent 当前场景的时间、空间、在场角色、环境特征 |
| 感知路由 | 将角色 A 的作用广播给能感知到的角色，信息量按感知能力分层 |
| 边界纠正 | agent 动作超出场景范围时（如林朝雨从柴房写到月光），叫停重来 |
| 方向覆盖 | agent 的方向与大纲冲突时（如符华追问而非纠正），导演覆盖 |
| 跑偏拒绝 | agent 输出明显错误（如瑶笙喊"爸"），拒绝并提示重来 |
| 互动记录 | 将全部轮次的原始互动数据存档，供后续精修参考 |
| 行文编织 | 运用元 skill 约束将互动数据组织为叙事文本 |
| 弧光变更 | 每章结束后记录各角色的弧光状态变化 |

---

## 何时使用 / 何时不使用

**使用**：写新场景的首次互动推演——尤其是多角色、情感密度高、角色认知差异大的场景。

**不使用**：
- 已经跑过推演、进入精修环节的场景（此时只需编辑和导演调文）
- 单角色叙述段落、环境描写、时间过渡
- 编剧已有确定写法的关键情节节点（直接写，不绕远路）

---

## 与元 skill 的对接

本 skill 注册于 `literary-fanfic-orchestrator` 的阶段 5（场景写作）——作为 `scene-craft` 的前置步骤，在需要时调用：

```
阶段 5：场景写作
├── ensemble-deduction（角色互动推演，按需）
├── scene-craft（场景结构）
├── dialogue-mastery（对话）
└── beautiful-prose（句子抛光）
```
